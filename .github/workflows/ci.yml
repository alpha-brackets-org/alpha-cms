# name: CI
#
# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]
#
# jobs:
#   quality:
#     name: Lint, Type-check & Build
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Lint
#         run: npm run lint
#
#       - name: Type-check
#         run: npx tsc --noEmit
#
#       - name: Build
#         run: npm run build
#         env:
#           # Provide stub env vars so Next.js build doesn't crash on missing vars
#           DATABASE_URL: mongodb://localhost:27017/test
#           AUTH_SECRET: ci_stub_secret_at_least_32_chars_long
#           ENCRYPTION_KEY: ci_stub_encryption_key_32_chars!!
#           NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY: stub
#           NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT: https://ik.imagekit.io/stub
#           IMAGEKIT_PRIVATE_KEY: stub
#           NEXT_PUBLIC_HUB_URL: http://localhost:3001
#           MAIL_HOST: localhost
#           MAIL_PORT: '1025'
#           MAIL_USER: stub
#           MAIL_PASS: stub
#           REDIS_URL: redis://localhost:6379
#
#   test:
#     name: Tests
#     runs-on: ubuntu-latest
#     # Remove 'if: false' once you add test files
#     if: false
#
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#       - run: npm ci
#       - run: npm test
